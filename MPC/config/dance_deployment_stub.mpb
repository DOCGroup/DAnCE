// -*- MPC -*-
project : pi, messaging, ifr_client, dance_logger {
  after += Deployment_stub
  libs  += DAnCE_Deployment_stub
  includes += $(DANCE_ROOT)
  idlflags += -I$(DANCE_ROOT)
  requires += threads
}
